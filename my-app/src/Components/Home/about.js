function About(props) {
  // const user = props.obj;
  // const bday = user.birthday.slice(0, 10);

  return (
    <div className="card-body mb-3">
      {/* <p className="mb-0 text-center">Gender: {user.gender}</p>
      {user.medicaid === false && (
        <>
          <p className="mb-0 text-center">Has Medicaid: No</p>
        </>
      )}
      {user.medicaid === true && (
        <>
          <p className="mb-0 text-center">Has Medicaid: Yes</p>
        </>
      )}
      <p className="mb-0 text-center">Birthday: {bday}</p> */}
    </div>
  );
}

export default About;
